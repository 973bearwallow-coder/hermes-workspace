import { type AppConfig } from "../../config.js";
import type { Logger } from "../../logger.js";
import { type ClientMessage } from "../../domain/protocol/client-protocol.js";
import type { ClientConnectionPort } from "./ports/client-connection.port.js";
import type { HermesRunsPort } from "./ports/hermes-runs.port.js";
import type { TaskSupervisorPort } from "./ports/task-supervisor.port.js";
import { type LiveModelAdapter } from "./ports/realtime-model.port.js";
export interface LiveGatewaySessionDeps {
    config: AppConfig;
    hermes: HermesRunsPort;
    taskSupervisor: TaskSupervisorPort;
    liveModel: LiveModelAdapter;
    logger: Logger;
}
export declare class LiveGatewaySession {
    private readonly client;
    private readonly deps;
    private readonly id;
    private readonly notificationToken;
    private readonly abort;
    private liveSession?;
    private pendingLiveConnect?;
    private starting;
    private readySent;
    private closing;
    private closePromise?;
    private sessionKey?;
    private ownerId?;
    private profileId;
    private userLabel;
    private protocolVersion;
    private conversation;
    private conversationOperation;
    private unsubscribeTasks?;
    private readonly pendingTaskRecords;
    private readonly pendingNotifications;
    private readonly claimedNotifications;
    private readonly notificationDeliveryAttempts;
    private notificationFlushRunning;
    private notificationRetryTimer?;
    private notificationResponsePending;
    private notificationResponseTimer?;
    private providerResponseActive;
    private providerTurnResponseExpected;
    private userSpeaking;
    private messageQueue;
    private pendingClientMessages;
    private pendingClientBytes;
    private clientInputOverflowed;
    private clientMessageErrors;
    private readonly providerToolCalls;
    private readonly providerToolCallTombstones;
    private readonly providerToolOperations;
    private activeProviderToolOperations;
    private pendingProviderToolCalls;
    private cachedProviderToolResponseBytes;
    constructor(client: ClientConnectionPort, deps: LiveGatewaySessionDeps);
    bind(): void;
    start(message: Extract<ClientMessage, {
        type: "session.start";
    }>): Promise<void>;
    close(): Promise<void>;
    private enqueueClientFrame;
    private handleClientMessage;
    private resolveConversation;
    private serializeConversationOperation;
    private availableProviderTools;
    private executeToolCall;
    private enqueueProviderToolCall;
    private handleProviderToolCallCancellation;
    private deliverProviderToolResponse;
    private scheduleProviderToolOperation;
    private drainProviderToolOperations;
    private failProviderToolQueueOverflow;
    private failExpiredProviderToolCallReplay;
    private failProviderToolReplayLedgerOverflow;
    private dispatchLiveModelEvent;
    private handleLiveModelEvent;
    private receiveTaskRecord;
    private dispatchTaskRecord;
    private scheduleNotificationFlush;
    private flushNotifications;
    private releaseNotificationClaim;
    private retryNotification;
    private scheduleNotificationRetry;
    private forwardRealtimeClientInput;
    private cancelRealtimeResponse;
    private performClose;
    private closeProvider;
    private closeClientAfterCleanup;
    private send;
    private handleClientMessageFailure;
    private fail;
    private failPublic;
    private runTaskOperation;
    private armNotificationResponseWatchdog;
    private clearNotificationResponsePending;
}
//# sourceMappingURL=live-gateway-session.d.ts.map